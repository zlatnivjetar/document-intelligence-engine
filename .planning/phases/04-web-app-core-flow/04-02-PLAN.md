---
phase: 04-web-app-core-flow
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/app/page.tsx
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/card.tsx
  - apps/web/src/components/ui/input.tsx
  - apps/web/src/components/ui/label.tsx
  - apps/web/src/components/ui/select.tsx
  - apps/web/src/lib/utils.ts
autonomous: true
requirements: [INPUT-04, WEB-08]

must_haves:
  truths:
    - "The web app has Tailwind v4 wired into Next.js and a shadcn-compatible component baseline"
    - "The page shell matches the Phase 4 UI-SPEC with the upload dropzone as the dominant above-the-fold element"
    - "The static layout already reserves visible zones for API key entry, template selection, extraction CTA, and result preview"
    - "No extraction behavior is implemented here yet; this plan only establishes the visual and component foundation"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "Tailwind import, color tokens, and typography variables from the UI contract"
    - path: "apps/web/src/app/page.tsx"
      provides: "Static one-page DocPipe workspace shell"
    - path: "apps/web/components.json"
      provides: "shadcn new-york configuration"
  key_links:
    - from: "apps/web/src/app/page.tsx"
      to: ".planning/phases/04-web-app-core-flow/04-UI-SPEC.md"
      via: "copy, spacing, and color contract"
      pattern: "Extract document"
---

<objective>
Build the web app visual foundation before wiring any extraction behavior. Phase 4 needs a polished, intentional single-page shell with Tailwind and shadcn primitives, otherwise the later interactive plan will turn into ad-hoc styling and churn. This plan locks the app shell, typography, colors, and layout so the extraction flow can be layered in cleanly.

Purpose: INPUT-04 and WEB-08 start with visible upload and template-selection surfaces. This plan creates those surfaces without mixing in API-key persistence or extraction state management.
Output: Tailwind + shadcn wiring in `apps/web`, global theme tokens, and a static one-page DocPipe shell.
</objective>

<execution_context>
@C:/Users/david/Desktop/github/document-intelligence-engine/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/Desktop/github/document-intelligence-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-app-core-flow/04-UI-SPEC.md

<interfaces>
```typescript
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
```

```tsx
<main className="min-h-screen bg-[var(--color-bg)] text-[var(--color-ink)]">
  <section className="mx-auto grid max-w-6xl gap-8 px-6 py-12 lg:grid-cols-[1.2fr,0.8fr]">
    <div>{/* upload + api key + template */}</div>
    <div>{/* result preview */}</div>
  </section>
</main>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind v4 and shadcn-compatible UI primitives in apps/web</name>
  <files>apps/web/package.json, apps/web/postcss.config.mjs, apps/web/components.json, apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/card.tsx, apps/web/src/components/ui/input.tsx, apps/web/src/components/ui/label.tsx, apps/web/src/components/ui/select.tsx, apps/web/src/lib/utils.ts</files>

  <read_first>
    - apps/web/package.json
    - apps/web/tsconfig.json
    - apps/web/src/app/layout.tsx
  </read_first>

  <action>
Update `apps/web/package.json` to add these exact dependencies:
```json
"dependencies": {
  "@docpipe/core": "workspace:*",
  "class-variance-authority": "^0.7.1",
  "clsx": "^2.1.1",
  "lucide-react": "^0.511.0",
  "next": "16.1.0",
  "react": "^19.0.0",
  "react-dom": "^19.0.0",
  "tailwind-merge": "^3.3.0"
},
"devDependencies": {
  "@tailwindcss/postcss": "^4.1.0",
  "@types/node": "^22.0.0",
  "@types/react": "^19.0.0",
  "@types/react-dom": "^19.0.0",
  "tailwindcss": "^4.1.0",
  "typescript": "^5.8.0"
}
```

Create `apps/web/postcss.config.mjs`:
```javascript
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
```

Create `apps/web/components.json` with this exact shape:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils"
  },
  "iconLibrary": "lucide"
}
```

Create `apps/web/src/lib/utils.ts` with:
```typescript
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

Create shadcn-style UI primitives that use `cn()` and `class-variance-authority`:
- `button.tsx`
- `card.tsx`
- `input.tsx`
- `label.tsx`
- `select.tsx`

Use the official composition shape: `Button`, `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `Input`, `Label`, `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`.
  </action>

  <verify>
    <automated>cd C:/Users/david/Desktop/github/document-intelligence-engine && pnpm install && pnpm --filter web type-check</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "@tailwindcss/postcss" apps/web/package.json` returns the Tailwind v4 PostCSS package
    - `apps/web/postcss.config.mjs` exists
    - `grep -n "\"style\": \"new-york\"" apps/web/components.json` returns the shadcn preset
    - `grep -n "export function cn" apps/web/src/lib/utils.ts` returns the utility function
    - `apps/web/src/components/ui/button.tsx` exists
    - `apps/web/src/components/ui/select.tsx` exists
    - `pnpm --filter web type-check` exits 0
  </acceptance_criteria>

  <done>apps/web has Tailwind v4, shadcn configuration, and reusable UI primitives ready for the Phase 4 shell.</done>
</task>

<task type="auto">
  <name>Task 2: Build the static single-page DocPipe shell from the Phase 4 UI contract</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/globals.css, apps/web/src/app/page.tsx</files>

  <read_first>
    - apps/web/src/app/layout.tsx
    - apps/web/src/app/page.tsx
    - .planning/phases/04-web-app-core-flow/04-UI-SPEC.md
    - apps/web/src/components/ui/button.tsx
    - apps/web/src/components/ui/card.tsx
    - apps/web/src/components/ui/input.tsx
    - apps/web/src/components/ui/select.tsx
  </read_first>

  <action>
Update `apps/web/src/app/layout.tsx` to load these exact fonts from `next/font/google`:
```typescript
Space_Grotesk
Source_Sans_3
```
Apply them through CSS variables on `<html>` and `<body>`.

Create `apps/web/src/app/globals.css` with:
1. `@import "tailwindcss";`
2. CSS custom properties for the UI contract:
```css
:root {
  --color-bg: #f4efe6;
  --color-surface: #d9ccb8;
  --color-card: #fbf8f2;
  --color-ink: #1f1a17;
  --color-muted: #63594e;
  --color-accent: #c8643b;
  --color-danger: #b42318;
  --radius-lg: 28px;
  --radius-md: 18px;
  --shadow-card: 0 20px 60px rgba(72, 47, 28, 0.12);
}
```
3. Base rules that set body background, heading font to Space Grotesk, and body font to Source Sans 3.

Replace `apps/web/src/app/page.tsx` with a static shell that renders:
- A display heading with the word `DocPipe`
- Supporting copy: `Drop a document to start`
- A large dropzone-style card with the text `PDF, PNG, or JPG`
- An API key card with label `Anthropic API key`
- A template selector card with label `Template`
- A primary CTA button with exact text `Extract document`
- A result preview card with heading `Result preview` and placeholder JSON block

Use a two-column desktop grid and a single-column mobile stack. Keep this file static: no `use client`, no event handlers, no extraction logic yet.
  </action>

  <verify>
    <automated>cd C:/Users/david/Desktop/github/document-intelligence-engine && pnpm --filter web build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "Space_Grotesk\\|Source_Sans_3" apps/web/src/app/layout.tsx` returns both fonts
    - `grep -n -- "--color-accent: #c8643b" apps/web/src/app/globals.css` returns the accent token
    - `grep -n "Drop a document to start" apps/web/src/app/page.tsx` returns the empty-state heading
    - `grep -n "Extract document" apps/web/src/app/page.tsx` returns the primary CTA copy
    - `grep -n "Result preview" apps/web/src/app/page.tsx` returns the result card heading
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>

  <done>The web app has a static, on-brand shell with Tailwind styling, layout structure, and visible Phase 4 surfaces ready for behavior wiring.</done>
</task>

</tasks>

<verification>
After both tasks:
- `pnpm --filter web type-check` exits 0
- `pnpm --filter web build` exits 0
- Tailwind tokens in `globals.css` match the UI-SPEC values
- The app shell renders upload, key, template, CTA, and result-preview zones on one page
</verification>

<success_criteria>
1. Tailwind v4 and shadcn-compatible components are present in apps/web
2. The page shell follows the approved UI contract for typography, color, and layout
3. Upload, key, template, CTA, and result preview all exist visually before behavior wiring starts
4. The web package still builds cleanly after the UI foundation pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-app-core-flow/04-02-SUMMARY.md`
</output>
