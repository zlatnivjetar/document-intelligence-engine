---
phase: 03-core-completeness
plan: "06"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - packages/core/test/fixtures/pdf/text-layer-routing.pdf
  - packages/core/test/fixtures/pdf/image-only-routing.pdf
  - packages/core/src/pdf-router.ts
  - packages/core/src/extract.ts
  - packages/core/src/pdf-routing.integration.test.ts
autonomous: true
requirements: [INPUT-03]
gap_closure: true

must_haves:
  truths:
    - "A real text-layer PDF takes a different processing path from a real image-only PDF."
    - "Text-layer PDFs are converted into a text prompt before the model call instead of being sent through the PDF file part path."
    - "Image-only PDFs stay on the existing PDF file-part path."
    - "Both routing cases are proven with committed PDFs on disk and the real unpdf classifier, not mocked strings."
    - "extract() still returns `pdfType` for both paths and keeps non-PDF inputs unchanged."
  artifacts:
    - path: "packages/core/test/fixtures/pdf/text-layer-routing.pdf"
      provides: "Committed text-layer routing fixture with >50 non-whitespace characters recoverable by unpdf"
    - path: "packages/core/test/fixtures/pdf/image-only-routing.pdf"
      provides: "Committed image-only routing fixture with no usable text layer"
    - path: "packages/core/src/pdf-router.ts"
      provides: "Routing analysis that yields pdfType plus extracted text for text-layer PDFs"
    - path: "packages/core/src/extract.ts"
      provides: "Distinct text-layer-vs-image-only processing branches before generateObject()"
    - path: "packages/core/src/pdf-routing.integration.test.ts"
      provides: "Fixture-backed routing tests that assert transport shape and result metadata"
  key_links:
    - from: "packages/core/src/pdf-router.ts"
      to: "unpdf.extractText"
      via: "extractText(new Uint8Array(pdfBuffer), { mergePages: true })"
      pattern: "mergePages: true"
    - from: "packages/core/src/extract.ts"
      to: "packages/core/src/pdf-router.ts"
      via: "analyzePdfRouting(pdfBuffer)"
      pattern: "analyzePdfRouting"
    - from: "packages/core/src/pdf-routing.integration.test.ts"
      to: "packages/core/test/fixtures/pdf/image-only-routing.pdf"
      via: "readFile(new URL('../test/fixtures/pdf/image-only-routing.pdf', import.meta.url))"
      pattern: "image-only-routing.pdf"
---

<objective>
Close the INPUT-03 verification gap by implementing a real routing split: text-layer PDFs use an extracted-text prompt path, while image-only PDFs continue through the PDF file/document path. Prove both branches with committed PDF fixtures and end-to-end integration tests.

Purpose: Phase 03 currently classifies PDFs but does not actually route them differently. This plan makes the roadmap literal again instead of downgrading the requirement to metadata-only annotation.
Output: Two committed routing fixtures, a routing-analysis seam in `pdf-router.ts`, a real branching path in `extract.ts`, and integration tests that prove the model request shape differs for each PDF type.
</objective>

<execution_context>
@C:/Users/david/Desktop/github/document-intelligence-engine/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/david/Desktop/github/document-intelligence-engine/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-completeness/03-02-SUMMARY.md
@.planning/phases/03-core-completeness/03-03-SUMMARY.md
@packages/core/src/pdf-router.ts
@packages/core/src/extract.ts
@packages/core/src/extract.test.ts

<interfaces>
From `packages/core/src/pdf-router.ts` today:
```typescript
export type PdfType = 'text-layer' | 'image-only';
export async function detectPdfType(pdfBuffer: Buffer): Promise<PdfType>;
```

From `packages/core/src/extract.ts` today:
```typescript
export interface ExtractOptions<T> {
  input: ExtractionInput;
  schema: z.ZodSchema<T>;
  model: LanguageModelV3;
  schemaName?: string;
  schemaDescription?: string;
  routingOverride?: PdfType;
  validators?: Array<(data: T) => ExtractionWarning[]>;
}
```

From `packages/core/node_modules/unpdf/dist/index.d.ts`:
```typescript
declare function extractText(
  data: DocumentInitParameters['data'] | PDFDocumentProxy,
  options: { mergePages: true }
): Promise<{ totalPages: number; text: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add committed routing PDF fixtures for real text-layer and image-only cases</name>
  <files>packages/core/test/fixtures/pdf/text-layer-routing.pdf, packages/core/test/fixtures/pdf/image-only-routing.pdf</files>
  <read_first>
    - packages/core/src/pdf-router.ts
    - packages/core/src/extract.ts
    - packages/core/node_modules/unpdf/dist/index.d.ts
    - packages/core/package.json
  </read_first>
  <action>
Create `packages/core/test/fixtures/pdf/` and commit two PDFs:

1. `text-layer-routing.pdf`
Create a one-page text-searchable PDF whose extracted text contains all of these exact strings:
- `Routing Sample Invoice`
- `Vendor Example Co`
- `Invoice Date: 2024-04-09`
- `Subtotal: 40.00`
- `Tax: 2.00`
- `Total: 42.00`

The merged extracted text must contain at least 50 non-whitespace characters so the classifier has an unambiguous text-layer result.

2. `image-only-routing.pdf`
Create a one-page PDF that visually contains invoice-like content but does not expose a usable text layer. The page content must be raster/image-based, not text operators. Target `unpdf.extractText(..., { mergePages: true })` output of either an empty string or fewer than 50 non-whitespace characters so the classifier lands on `image-only`.

If you use any local helper script to generate the PDFs, keep the committed `.pdf` artifacts in git and remove any temporary scratch output. The gap is not closed by a generator alone; the repo must contain the real fixtures at the paths above.
  </action>
  <verify>
    <automated>pnpm --filter @docpipe/core exec node --input-type=module -e "import { readFile } from 'node:fs/promises'; import { extractText } from 'unpdf'; const textPdf = await readFile('packages/core/test/fixtures/pdf/text-layer-routing.pdf'); const text = await extractText(new Uint8Array(textPdf), { mergePages: true }); if (!text.text.includes('Routing Sample Invoice') || text.text.replace(/\\s/g, '').length < 50) throw new Error('text-layer routing fixture failed'); const imagePdf = await readFile('packages/core/test/fixtures/pdf/image-only-routing.pdf'); const image = await extractText(new Uint8Array(imagePdf), { mergePages: true }); if (image.text.replace(/\\s/g, '').length >= 50) throw new Error('image-only routing fixture still has a text layer');"</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path packages/core/test/fixtures/pdf/text-layer-routing.pdf` returns `True`
    - `Test-Path packages/core/test/fixtures/pdf/image-only-routing.pdf` returns `True`
    - The `<automated>` command succeeds, proving one fixture exceeds the text-layer threshold and the other stays below it
    - `pnpm --filter @docpipe/core exec node --input-type=module -e "import { readFile } from 'node:fs/promises'; import { extractText } from 'unpdf'; const pdf = await readFile('packages/core/test/fixtures/pdf/text-layer-routing.pdf'); const result = await extractText(new Uint8Array(pdf), { mergePages: true }); if (!result.text.includes('Vendor Example Co')) throw new Error('missing text-layer marker');"` exits `0`
  </acceptance_criteria>
  <done>Both routing fixtures are committed and deterministically classify as text-layer versus image-only under the real unpdf extractor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the real routing split and prove each branch with fixture-backed integration tests</name>
  <files>packages/core/src/pdf-router.ts, packages/core/src/extract.ts, packages/core/src/pdf-routing.integration.test.ts</files>
  <read_first>
    - packages/core/src/pdf-router.ts
    - packages/core/src/extract.ts
    - packages/core/src/extract.test.ts
    - packages/core/test/fixtures/pdf/text-layer-routing.pdf
    - packages/core/test/fixtures/pdf/image-only-routing.pdf
  </read_first>
  <acceptance_criteria>
    - `Select-String -Path packages/core/src/pdf-router.ts -Pattern 'analyzePdfRouting'` returns a match
    - `Select-String -Path packages/core/src/pdf-router.ts -Pattern 'extractedText'` returns a match
    - `Select-String -Path packages/core/src/extract.ts -Pattern 'text-layer PDF'` returns a match for the text-route prompt copy
    - `Select-String -Path packages/core/src/pdf-routing.integration.test.ts -Pattern 'contains no file part'` returns a match
    - `Select-String -Path packages/core/src/pdf-routing.integration.test.ts -Pattern 'contains a file part with mediaType'` returns a match
    - `pnpm --filter @docpipe/core test -- src/pdf-routing.integration.test.ts` exits `0`
    - `pnpm --filter @docpipe/core test` exits `0`
    - `pnpm --filter @docpipe/core type-check` exits `0`
    - `pnpm exec turbo build` exits `0`
  </acceptance_criteria>
  <action>
Refactor `packages/core/src/pdf-router.ts` to add an explicit routing-analysis seam with this exact name and contract:

```typescript
export interface PdfRoutingAnalysis {
  pdfType: PdfType;
  extractedText?: string;
}

export async function analyzePdfRouting(
  pdfBuffer: Buffer,
): Promise<PdfRoutingAnalysis>
```

Implementation requirements:
- Call `extractText(new Uint8Array(pdfBuffer), { mergePages: true })`.
- Normalize the merged text with `trim()` and count non-whitespace characters.
- Return `{ pdfType: 'text-layer', extractedText: normalizedText }` only when the threshold is met.
- Return `{ pdfType: 'image-only' }` when the text layer is absent, too short, or extraction throws.
- Keep `detectPdfType()` as a thin wrapper over `analyzePdfRouting()` so existing public API stays intact.

Update `packages/core/src/extract.ts` so PDF inputs genuinely branch:
- For analyzed `text-layer` PDFs, do not send a `{ type: 'file' }` part to `generateObject`.
- Instead, build the user content as text-only parts:
  - part 1: `Extract structured data from this text-layer PDF. Use the extracted text exactly as provided.`
  - part 2: the `extractedText` returned by `analyzePdfRouting()`
- For analyzed `image-only` PDFs, preserve the current file-part path unchanged.
- Preserve current PNG/JPEG behavior unchanged.
- Preserve `routingOverride` for deterministic metadata-oriented tests, but do not let it erase the real branch for fixture-driven tests when actual extracted text is available.

Create `packages/core/src/pdf-routing.integration.test.ts`. Mock only `generateObject`; do not mock `unpdf`.

Add these tests with these exact names:
- `text-layer routing fixture: extract() uses extracted PDF text and contains no file part`
- `image-only routing fixture: extract() keeps the PDF file part path`

For the text-layer test:
- Load `packages/core/test/fixtures/pdf/text-layer-routing.pdf` from disk.
- Call `analyzePdfRouting()` first and assert `pdfType === 'text-layer'` and `extractedText` contains `Routing Sample Invoice`.
- Mock `generateObject` to return `{ vendor: 'Vendor Example Co', total: 42 }` plus confidence values.
- Call `extract()` with the real PDF bytes and a simple test schema.
- Assert `result.pdfType === 'text-layer'`.
- Assert the captured `generateObject` request has no `content` item with `type === 'file'`.
- Assert one text part contains `Routing Sample Invoice`.

For the image-only test:
- Load `packages/core/test/fixtures/pdf/image-only-routing.pdf` from disk.
- Call `analyzePdfRouting()` first and assert `pdfType === 'image-only'`.
- Mock `generateObject` to return `{ vendor: 'Vendor Example Co', total: 42 }` plus confidence values.
- Call `extract()` with the real PDF bytes and the same test schema.
- Assert `result.pdfType === 'image-only'`.
- Assert the captured `generateObject` request still includes a `file` part with `mediaType: 'application/pdf'`.

Keep the existing `extract.test.ts` routingOverride tests passing; this change adds a new observable branch, it does not remove the current metadata annotations.
  </action>
  <verify>
    <automated>pnpm --filter @docpipe/core test -- src/pdf-routing.integration.test.ts</automated>
  </verify>
  <done>Real text-layer PDFs now take a text extraction route, real image-only PDFs stay on the file route, and both branches are proven with committed fixtures and integration tests.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @docpipe/core test -- src/pdf-routing.integration.test.ts`
- `pnpm --filter @docpipe/core test`
- `pnpm --filter @docpipe/core type-check`
- `pnpm exec turbo build`
</verification>

<success_criteria>
1. `packages/core/src/extract.ts` no longer treats all PDFs identically; text-layer and image-only PDFs go through different model-input paths.
2. `packages/core/src/pdf-routing.integration.test.ts` proves the difference by asserting request-shape changes against real PDFs on disk.
3. `packages/core/test/fixtures/pdf/text-layer-routing.pdf` and `packages/core/test/fixtures/pdf/image-only-routing.pdf` are committed and deterministically classify to opposite routing outcomes.
4. `result.pdfType` remains available on both branches, so callers still receive routing metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-completeness/03-06-SUMMARY.md`
</output>
