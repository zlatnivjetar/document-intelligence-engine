---
phase: 03-core-completeness
plan: "05"
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - packages/core/test/fixtures/receipt/known-answer.pdf
  - packages/core/test/fixtures/receipt/expected.json
  - packages/core/test/fixtures/w2/known-answer.pdf
  - packages/core/test/fixtures/w2/expected.json
  - packages/core/src/template-fixtures.test.ts
autonomous: true
requirements: [TMPL-02, TMPL-03]
gap_closure: true

must_haves:
  truths:
    - "Receipt coverage is backed by a committed known-answer document fixture, not only inline mocked objects."
    - "W-2 coverage is backed by a committed known-answer document fixture, not only inline mocked objects."
    - "Fixture tests assert both exact extracted values and exact confidence labels for receipt and W-2 outputs."
    - "The fixture documents are real text-layer PDFs: unpdf extracts their text and extract() annotates them as pdfType 'text-layer'."
  artifacts:
    - path: "packages/core/test/fixtures/receipt/known-answer.pdf"
      provides: "Committed receipt PDF fixture with selectable text matching the receipt expectation manifest"
    - path: "packages/core/test/fixtures/receipt/expected.json"
      provides: "Known-answer receipt data and confidence manifest"
    - path: "packages/core/test/fixtures/w2/known-answer.pdf"
      provides: "Committed W-2 PDF fixture with selectable text matching the W-2 expectation manifest"
    - path: "packages/core/test/fixtures/w2/expected.json"
      provides: "Known-answer W-2 data and confidence manifest"
    - path: "packages/core/src/template-fixtures.test.ts"
      provides: "Fixture-backed integration tests for receiptSchema and w2Schema"
  key_links:
    - from: "packages/core/src/template-fixtures.test.ts"
      to: "packages/core/test/fixtures/receipt/known-answer.pdf"
      via: "readFile(new URL('../test/fixtures/receipt/known-answer.pdf', import.meta.url))"
      pattern: "known-answer.pdf"
    - from: "packages/core/src/template-fixtures.test.ts"
      to: "packages/core/src/extract.ts"
      via: "extract({ input: { document: fixtureBytes, mimeType: 'application/pdf' }, schema, model })"
      pattern: "mimeType: 'application/pdf'"
    - from: "packages/core/src/template-fixtures.test.ts"
      to: "packages/core/test/fixtures/w2/expected.json"
      via: "JSON.parse(readFile(...expected.json))"
      pattern: "expected.json"
---

<objective>
Close the TMPL-02 and TMPL-03 verification gap by adding committed receipt and W-2 known-answer document fixtures plus fixture-driven integration tests that assert exact extracted values and exact confidence labels.

Purpose: Phase 03 already proved schema shape and mocked template wiring. This plan adds the missing artifact-level evidence the roadmap still requires: real fixture documents on disk with known expected outputs.
Output: Two committed PDF fixtures, two expectation manifests, and one fixture-backed test file that exercises `extract()` with real document bytes.
</objective>

<execution_context>
@C:/Users/david/Desktop/github/document-intelligence-engine/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/david/Desktop/github/document-intelligence-engine/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-completeness/03-01-SUMMARY.md
@.planning/phases/03-core-completeness/03-02-SUMMARY.md
@.planning/phases/03-core-completeness/03-03-SUMMARY.md
@packages/core/src/templates/receipt.ts
@packages/core/src/templates/w2.ts
@packages/core/src/extract.ts
@packages/core/src/extract.test.ts

<interfaces>
From `packages/core/src/templates/receipt.ts`:
```typescript
export const receiptSchema: z.ZodObject<ReceiptSchemaShape>;
export type ReceiptData = z.infer<typeof receiptSchema>;
```

From `packages/core/src/templates/w2.ts`:
```typescript
export const w2Schema: z.ZodObject<W2SchemaShape>;
export type W2Data = z.infer<typeof w2Schema>;
```

From `packages/core/src/types.ts`:
```typescript
export interface ExtractionResult<T> {
  data: T;
  confidence: Record<string, number>;
  overallConfidence: number;
  pdfType?: 'text-layer' | 'image-only';
  warnings?: ExtractionWarning[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add committed receipt and W-2 known-answer PDF fixtures with expectation manifests</name>
  <files>packages/core/test/fixtures/receipt/known-answer.pdf, packages/core/test/fixtures/receipt/expected.json, packages/core/test/fixtures/w2/known-answer.pdf, packages/core/test/fixtures/w2/expected.json</files>
  <read_first>
    - packages/core/src/templates/receipt.ts
    - packages/core/src/templates/w2.ts
    - packages/core/src/extract.test.ts
    - packages/core/package.json
    - packages/core/node_modules/unpdf/dist/index.d.ts
  </read_first>
  <action>
Create a new test-only fixture tree under `packages/core/test/fixtures/` instead of `src/` so these artifacts do not ship in the npm tarball.

Create `packages/core/test/fixtures/receipt/expected.json` with this exact top-level shape:
- `data`: TMPL-02 receipt object using the schema field names exactly.
- `confidence`: object keyed by the same 7 receipt top-level fields.

Set the receipt manifest values exactly to:
- `merchantName`: `Corner Cafe`
- `merchantAddress`: `42 Oak Street, Portland, OR 97201`
- `date`: `2024-03-15`
- `lineItems`: `[{"description":"Latte","quantity":1,"unitPrice":5.5,"amount":5.5}]`
- `subtotal`: `5.5`
- `tax`: `0.55`
- `total`: `6.05`
- `confidence.merchantName`: `0.99`
- `confidence.merchantAddress`: `0.98`
- `confidence.date`: `0.95`
- `confidence.lineItems`: `0.97`
- `confidence.subtotal`: `0.96`
- `confidence.tax`: `0.95`
- `confidence.total`: `0.97`

Create `packages/core/test/fixtures/w2/expected.json` with this exact top-level shape:
- `data`: TMPL-03 W-2 object using the schema field names exactly.
- `confidence`: object keyed by the same 12 W-2 top-level fields.

Set the W-2 manifest values exactly to:
- `employerName`: `Acme Corporation`
- `employerAddress`: `100 Corporate Blvd, Chicago, IL 60601`
- `employerEin`: `12-3456789`
- `employeeName`: `Jane Smith`
- `employeeAddress`: `456 Elm Ave, Chicago, IL 60602`
- `employeeSsn`: `XXX-XX-1234`
- `taxYear`: `2023`
- `wagesTipsOtherComp`: `75000`
- `federalIncomeTaxWithheld`: `12500`
- `stateWages`: `75000`
- `stateTaxWithheld`: `4500`
- `stateCode`: `IL`
- `confidence.employerName`: `0.98`
- `confidence.employerAddress`: `0.97`
- `confidence.employerEin`: `0.96`
- `confidence.employeeName`: `0.98`
- `confidence.employeeAddress`: `0.97`
- `confidence.employeeSsn`: `0.95`
- `confidence.taxYear`: `0.99`
- `confidence.wagesTipsOtherComp`: `0.95`
- `confidence.federalIncomeTaxWithheld`: `0.95`
- `confidence.stateWages`: `0.95`
- `confidence.stateTaxWithheld`: `0.95`
- `confidence.stateCode`: `0.96`

Commit one real text-layer PDF at each `known-answer.pdf` path. The selectable text inside each PDF must match its JSON manifest verbatim so `unpdf.extractText(..., { mergePages: true })` can recover the same merchant/employer names, totals, and tax year. If you use a local generator script to produce the PDFs, keep the generated `.pdf` files in git; do not leave the repo with only a generator script and no committed fixtures.
  </action>
  <verify>
    <automated>pnpm --filter @docpipe/core exec node --input-type=module -e "import { readFile } from 'node:fs/promises'; import { extractText } from 'unpdf'; const receiptPdf = await readFile('packages/core/test/fixtures/receipt/known-answer.pdf'); const receipt = await extractText(new Uint8Array(receiptPdf), { mergePages: true }); if (!receipt.text.includes('Corner Cafe') || receipt.text.replace(/\\s/g, '').length < 50) throw new Error('receipt fixture is not a usable text-layer PDF'); const w2Pdf = await readFile('packages/core/test/fixtures/w2/known-answer.pdf'); const w2 = await extractText(new Uint8Array(w2Pdf), { mergePages: true }); if (!w2.text.includes('Acme Corporation') || !w2.text.includes('2023') || w2.text.replace(/\\s/g, '').length < 50) throw new Error('w2 fixture is not a usable text-layer PDF');"</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path packages/core/test/fixtures/receipt/known-answer.pdf` returns `True`
    - `Test-Path packages/core/test/fixtures/w2/known-answer.pdf` returns `True`
    - `Select-String -Path packages/core/test/fixtures/receipt/expected.json -Pattern '\"merchantName\": \"Corner Cafe\"'` returns a match
    - `Select-String -Path packages/core/test/fixtures/receipt/expected.json -Pattern '\"total\": 6.05'` returns a match
    - `Select-String -Path packages/core/test/fixtures/w2/expected.json -Pattern '\"employerName\": \"Acme Corporation\"'` returns a match
    - `Select-String -Path packages/core/test/fixtures/w2/expected.json -Pattern '\"wagesTipsOtherComp\": 75000'` returns a match
    - The `<automated>` command succeeds, proving both committed PDFs expose selectable text and exceed the 50-character text-layer threshold
  </acceptance_criteria>
  <done>Both template fixtures exist as committed text-layer PDFs with expectation manifests whose values match the visible document text exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture-backed receipt and W-2 integration tests with exact confidence assertions</name>
  <files>packages/core/src/template-fixtures.test.ts</files>
  <read_first>
    - packages/core/src/extract.ts
    - packages/core/src/extract.test.ts
    - packages/core/src/templates/receipt.ts
    - packages/core/src/templates/w2.ts
    - packages/core/test/fixtures/receipt/expected.json
    - packages/core/test/fixtures/w2/expected.json
  </read_first>
  <action>
Create `packages/core/src/template-fixtures.test.ts` as a dedicated fixture-backed Vitest file. Do not append these cases into `extract.test.ts`; keep the gap-closing coverage isolated.

In the new test file:
- Import `readFile` from `node:fs/promises`.
- Mock only `generateObject` from `ai`.
- Do not mock `unpdf` or `detectPdfType`; the real fixture PDFs must drive `pdfType` classification.
- Load each `known-answer.pdf` and `expected.json` from disk with `new URL('../test/fixtures/...', import.meta.url)`.

Add one receipt case and one W-2 case. In each case:
- Mock `generateObject` to return `object.extracted = expected.data` and `object.confidence = expected.confidence`.
- Call `extract({ input: { document: fixtureBytes, mimeType: 'application/pdf' }, schema, model: mockModel })` with no `routingOverride`.
- Assert `result.data` deep-equals `expected.data`.
- Assert `result.confidence` deep-equals `expected.confidence`.
- Assert `result.overallConfidence` equals the arithmetic mean of the manifest confidence values via `toBeCloseTo`.
- Assert `result.pdfType === 'text-layer'`.
- Assert `result.warnings` is `undefined`.

Name the tests so the requirement trace is explicit:
- `TMPL-02 fixture: receipt known-answer PDF returns exact values and confidence labels`
- `TMPL-03 fixture: W-2 known-answer PDF returns exact values and confidence labels`
  </action>
  <verify>
    <automated>pnpm --filter @docpipe/core test -- src/template-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path packages/core/src/template-fixtures.test.ts` returns `True`
    - `Select-String -Path packages/core/src/template-fixtures.test.ts -Pattern 'TMPL-02 fixture: receipt known-answer PDF returns exact values and confidence labels'` returns a match
    - `Select-String -Path packages/core/src/template-fixtures.test.ts -Pattern 'TMPL-03 fixture: W-2 known-answer PDF returns exact values and confidence labels'` returns a match
    - `Select-String -Path packages/core/src/template-fixtures.test.ts -Pattern 'pdfType'` returns at least one assertion line
    - `Select-String -Path packages/core/src/template-fixtures.test.ts -Pattern 'overallConfidence'` returns at least one assertion line
    - `pnpm --filter @docpipe/core test -- src/template-fixtures.test.ts` exits `0`
    - `pnpm --filter @docpipe/core test` exits `0`
  </acceptance_criteria>
  <done>The core test suite now includes fixture-backed receipt and W-2 cases that use committed PDFs and assert exact output values plus exact confidence labels.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @docpipe/core test -- src/template-fixtures.test.ts`
- `pnpm --filter @docpipe/core test`
- `pnpm --filter @docpipe/core type-check`
- `pnpm exec turbo build`
</verification>

<success_criteria>
1. Receipt and W-2 both have committed known-answer PDF fixtures under `packages/core/test/fixtures/`.
2. Each fixture has a committed JSON manifest for expected extracted values and expected confidence labels.
3. `packages/core/src/template-fixtures.test.ts` loads the real fixture bytes and proves `extract()` returns the manifest values for TMPL-02 and TMPL-03.
4. Both fixture cases verify `pdfType === 'text-layer'`, showing the fixtures are real text-layer PDFs rather than inert placeholder files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-completeness/03-05-SUMMARY.md`
</output>
